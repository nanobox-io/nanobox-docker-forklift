#!/bin/bash

. /opt/forklift/lib/forklift

# parse args and set values
for i in "${@}"; do

  case $i in
    build_id=* )
      BUILD_ID=${i#*=}
      ;;
    warehouse_token=* )
      WAREHOUSE_TOKEN=${i#*=}
      ;;
    warehouse_ip=* )
      WAREHOUSE_IP=${i#*=}
      ;;
  esac

done

fetch_image